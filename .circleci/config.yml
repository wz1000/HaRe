version: 2
jobs:
  build:
    docker:
      - image: alanz/hare-ci
    steps:
      - checkout
      - run: stack -j 2 --stack-yaml=stack-8.2.2.yaml setup
      - run: stack -j 2 --stack-yaml=stack-8.2.2.yaml test
